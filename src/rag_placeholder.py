def get_context(user_query):
    """
    Placeholder for future Retrieval-Augmented Generation.

    Later, this function can:
    - search preprocessed how-to articles,
    - retrieve relevant video captions or metadata,
    - select the top-k most relevant chunks,
    - limit the amount of retrieved text passed into the prompt.

    For now, retrieval is not implemented, so the context block stays empty.
    """

    return ""